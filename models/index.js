const UserModel = require("./user-model");
const BookModel = require("./book-model");

module.exports = { UserModel, BookModel };

// We have made this file because model can be so many if we don't keep them in one file
// we have to import all models in books.js separately

